<div class="row">
  <div class="col-lg-12 d-flex">
    <p class="h2 align-self-center p-2">Editoras</p>
    <%= link_to new_admins_backoffice_publisher_path, class:"btn btn-success btn-circle btn-sm align-self-center" do %>
      <i class="fa fa-plus"></i>
    <% end %>    
  </div>
</div>
<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover table-sm">
    <thead>
      <tr>
        <th><%= @publishers.model.human_attribute_name(:description) %></th>
        <th>            
        </th>
      </tr>
    </thead>
    <tbody>
      <% @publishers.each do |publisher| %>
      <tr>
        <td><%= publisher.description %></td>
        <td style="width: 90px">
          <%= link_to edit_admins_backoffice_publisher_path(publisher), 
            class:"btn btn-primary btn-circle btn-sm" do %>
            <i class="fa fa-edit"></i>
          <% end %>
          <%= link_to admins_backoffice_publisher_path(publisher), 
            method: :delete, 
            class:"btn btn-danger btn-circle btn-sm", 
            data: { confirm: "Tem certeza?"} do %>
            <i class="fa fa-minus"></i>
          <% end %>
          </button>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <%= paginate @publishers %>
</div>